Run the race-detector test suite and generate a coverage report:

```bash
make cover-html
```

`make cover-html` runs the race-detector tests, writes `cover.out`, then renders `cover.html`. Coverage percentage appears in the test output, e.g. `coverage: 87.3% of statements`.
