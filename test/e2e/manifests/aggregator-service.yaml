apiVersion: v1
kind: Service
metadata:
  name: aggregator-svc
  namespace: e2e-metrics-aggregator
spec:
  selector:
    app: aggregator-sidecar
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
      name: metrics
